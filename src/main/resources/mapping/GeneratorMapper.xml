<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.springbootv1.dao.IGeneratorDao">
    <select id="getTables" resultType="java.util.HashMap">
        SELECT
            `TABLE_NAME`,
            `TABLE_COMMENT`
        FROM
            information_schema.`TABLES`
        WHERE
            `TABLE_SCHEMA` = (SELECT DATABASE())
        ORDER BY `CREATE_TIME` DESC
    </select>

    <select id="getColumnsByTable" resultType="java.util.HashMap">
        SELECT
            `COLUMN_NAME`,`COLUMN_COMMENT`,`COLUMN_TYPE`,`TABLE_CATALOG`,`TABLE_SCHEMA`
        FROM
            information_schema.`COLUMNS`
        where TABLE_NAME = #{tableName}
        <if test="search != null">
            and (`COLUMN_NAME` like '%${search}%' or `COLUMN_COMMENT` like '%${search}%')
        </if>
        <if test="sort != null">
            order by ${sort}
        </if>
        <if test="sort == null">
            order by `COLUMN_NAME` asc
        </if>
    </select>
</mapper>